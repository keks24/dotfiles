[user]
    email = Ramon_Fischer@hotmail.de
    name = Ramon Fischer
    signingkey = 598398DA5F4DA46438FDCF87155BE26413E699BF
[include]
    path = ~/.gitconfig.d/gitalias.txt
[alias]
    callm = commit --all --message
    cv = commit --verbose
    d = diff --color-words
    dp = checkout --patch
    lt = lfs track
    ltt = lfs migrate info --everything --fixup
    lti = lfs migrate import --fixup --verbose
    # git config lfs.https://<some_fqdn>/keks24/<some_repository_name>/info/lfs.locksverify true
    rp = reset --patch
#[pull]
#    rebase = true
[commit]
    gpgsign = true
[filter "lfs"]
    process = git-lfs filter-process
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
